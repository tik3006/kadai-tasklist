<h1>new task</h1>

<%= form_with(model: @task, local: true) do |f| %>
	<%= f.label :content, 'task' %>
	<%= f.text_field :content %>
	
	 <%= f.submit 'addtask' %>
  
<% end %>

<%= link_to 'back', tasks_path %>